// src/defaultConfig.js
const defaultConfig = {
    maxTicketCapacity: 100,
    ticketsPerRelease: 10,
    ticketReleaseInterval: 30,
    customerRetrievalInterval: 20,
    vendorCount: 5,
    customerCount: 50,
    debug: false
};

export default defaultConfig;
